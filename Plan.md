# SignalChord

## コンセプト

複数の高さの音を同時に使って、別の端末へ情報を送る通信体験Webアプリ。

1つのWebアプリを**送信モード / 受信モード**に切り替えて使用する。

送信側では、送りたい情報を5つの周波数の音に変換してスピーカーから再生する。

受信側では、マイクでその音を聞き取り、周波数ごとの信号を解析して元の情報を復元する。

「たくさんの周波数を同時に使って情報を送る」という、5GやWi-Fiなどで使われているOFDMにつながる考え方を、聞いて・見て・実際に通信することで体験する。

---

## 教育目標

* 音や電波などの「波」を使って情報を送ることができることを知る。
* 高さの違う音は、それぞれ別の情報の通り道として使えることを知る。
* 複数の周波数を同時に使うことで、一度に複数の情報を送れることを体験する。
* 送信した波を受信側で解析することで、元の情報を取り出せることを知る。
* 5GやWi-Fiなどの通信でも、多数の周波数を利用して情報を送っていることを知る。

---

## ユーザー体験

1. アプリを起動し、**「送る」または「受けとる」**を選択する。
2. 送信側では、送りたい文字や情報を選択する。
3. 情報が5つの周波数に割り当てられる。
4. 画面には、使用する5つの周波数が横に並んで表示される。
5. 「送る」を押すと、5つの音のうち情報に対応する音が同時にスピーカーから鳴る。
6. 別の端末を受信モードにして、その音をマイクで聞く。
7. 受信側ではFFTによって音を周波数ごとに分け、それぞれの信号を読み取る。
8. 読み取った情報から、送信した文字や情報が画面上に復元される。
9. 送信側と受信側の周波数表示を見比べることで、どの周波数が情報を運んだのか確認できる。

---

## 通信方法

最初のプロトタイプでは、**Cメジャーペンタトニックの5音**を通信に使用する。

* C5：523.25 Hz
* D5：587.33 Hz
* E5：659.25 Hz
* G5：783.99 Hz
* A5：880.00 Hz

周波数を等間隔に並べるのではなく、5音を同時に鳴らしたときにも耳障りになりにくい音階を使用する。

それぞれの周波数を1つの情報の通り道として扱う。

最初は、

**音がある = 1**

**音がない = 0**

という単純な方式で情報を表す。

5つの周波数を同時に使用することで、**5bit = 32通り**の状態を一度に送る。

---

## 可視化

送信側・受信側の両方で、5つの音を横方向に並べて表示する。

例：

`C5 | D5 | E5 | G5 | A5`

使用している音や受信できた音が視覚的に反応する。

必要に応じてFFTによる周波数スペクトルも表示する。

音として聞くだけではなく、

**「複数の周波数が同時に情報を運んでいる」**

ことが画面からも分かるようにする。

---

## 発展的な体験

特定の周波数だけが届かない状況をつくり、通信への影響を確認できるようにする。

例えば、

`C5 | D5 | × | G5 | A5`

のように一つの周波数が失われると、情報の一部が正しく届かなくなる。

これによって、

**周波数ごとに別々の情報を運んでいる**

ことを体験できる。

---

## 実際の通信との関係

このアプリはOFDMそのものを完全に再現するものではなく、仕組みを理解するために単純化した教育用モデルとする。

実際の5GやWi-Fiでは、多数の搬送波を同時に使用し、それぞれの波の振幅や位相などを変化させることで情報を送っている。

このアプリでは、それを人間が直接聞くことのできる**5つの音の高さ**に置き換えて体験する。

---

## 設計方針

* ブラウザのみで動作する（GitHub Pages対応）
* TypeScriptベース
* スマートフォン・PCで動作可能にする
* Web Audio APIを使用する
* 1つのアプリで送信モード / 受信モードを切り替える
* 送信には端末のスピーカーを使用する
* 受信には端末のマイクを使用する
* FFTによって周波数成分を解析する
* 通信にはC5メジャーペンタトニックの5音を使用する
* 2台のスマートフォンだけでも通信実験ができるようにする
* 通信の仕組みと画面表示の対応関係をできるだけ単純にする
* OFDMの完全な再現よりも、教育的に理解しやすいことを優先する
* UIや説明はできるだけシンプルにする
* 不要な機能や華美な装飾は追加しない
