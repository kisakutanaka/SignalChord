# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクトの状態

このリポジトリは現在**企画・設計段階**であり、実装コード（`package.json`、`src/`等）はまだ存在しない。
`Plan.md`（このアプリの企画書）と `science-app-framework.md`（ScienceMuseum系アプリの共通開発フレームワーク）の
2つのMarkdownのみが置かれている。実装を開始する際は、まず `science-app-framework.md` の
「1. ドキュメント構成（3点セット）」に従い、`Step.md`（実装ロードマップ）と `Findings.md`
（科学的検証ログ）をこのCLAUDE.mdと合わせて作成すること。

ビルド・lint・テストのコマンドは実装が始まってから決まる。技術選定は下記「設計方針」に従う
（TypeScript、Web Audio API、ブラウザのみで完結、GitHub Pages対応）。実装が進んだら、
このセクションと以下の「アーキテクチャ」節を実際のコードに合わせて更新すること。

## SignalChord とは

複数の高さの音を同時に使って別の端末へ情報を送る通信体験Webアプリ（科学館向け教育展示）。
1つのWebアプリを**送信モード / 受信モード**に切り替えて使用し、送信側はスピーカーから、
受信側はマイクとFFTで、複数周波数を同時に使う通信（5G/Wi-FiのOFDMにつながる考え方）を
「聞いて・見て・実際に通信する」ことで体験させる。詳細な企画・UX・教育目標は `Plan.md` を参照。

### コア仕様（変更時は Plan.md も合わせて更新する）

- 通信には**Cメジャーペンタトニックの5音**を使用: C5 (523.25Hz) / D5 (587.33Hz) / E5 (659.25Hz) /
  G5 (783.99Hz) / A5 (880.00Hz)。等間隔な周波数ではなく、同時に鳴らしても耳障りになりにくい音階を選んでいる。
- 各周波数を1本の情報チャンネルとして扱い、「音がある=1 / 音がない=0」の単純な方式で
  5bit=32通りの状態を1回の送信で表現する（将来的な拡張の余地はあるが、最初のプロトタイプはこれ）。
- 可視化は送受信双方で5音を横並び表示し（`C5 | D5 | E5 | G5 | A5`）、使用中/受信できた音を視覚的に
  反応させる。周波数が欠落する状況（例: `C5 | D5 | × | G5 | A5`）を作り、通信への影響を確認できる
  発展的体験も企画に含まれる。

## 設計方針

`Plan.md` と `science-app-framework.md` 双方の「設計方針」節に基づく、このプロジェクトのデフォルト方針:

- **ブラウザのみで完結**させる（サーバーサイド処理に依存しない静的サイト。GitHub Pages等での
  無料ホストを前提にする）
- TypeScriptベース、Web Audio APIを使用（送信=スピーカー再生、受信=マイク入力+FFTによる周波数解析）
- スマートフォン・PC両方、特に**モバイル・タッチ操作を前提**に設計する（展示端末・来館者スマホでの
  利用、2台のスマートフォンだけで通信実験ができることを想定）
- 1つのアプリで送信モード/受信モードを切り替える構成にする
- **教育効果を最優先**する。機能追加や技術的な凝り方より学習目標への貢献度で判断する。
  通信の仕組みと画面表示の対応関係をできるだけ単純にし、OFDMの完全な再現よりも教育的理解しやすさを優先する
- UIや説明はできるだけシンプルにし、不要な機能や華美な装飾は追加しない
- 技術選定に迷ったら「ブラウザのみで完結」「モバイルで動く」「教育効果」を優先基準にする

## 開発の進め方（science-app-framework.md より）

このプロジェクトはGWPlayer（重力波プレイヤー）等の先行プロジェクトから一般化された共通フローに
従う。実装作業を行う際は以下を踏まえる。

- **まず一つの具体例で全機能を縦に通す（MVP）**。複数パターン対応などの横展開は、縦の筋
  （5音固定・1対1送受信）が動くことを確認してから着手する
- 各フェーズは「動くものが一つ増える」単位で区切り、都度動作確認する。**各フェーズの最後に
  実機確認ステップ**（デプロイ→実機（スマホ）でアクセス→確認）を入れる。まとめて最後にやらず、
  早期にデプロイして確認するサイクルを回す
- ユーザーからの調整依頼は都度 `Step.md` か実装コメントに反映し、経緯を追えるようにする
- 実装の途中でも「これは `Step.md` に追記すべきか」「これは `Findings.md` に残すべき発見
  （なぜこの周波数/しきい値なのか等）か」を自発的に判断し、都度反映する（指示を待たない）
- テスト・確認は**モックではなく実際にアプリを起動して**ブラウザ（可能ならPlaywrightで実機/実音響に
  近い環境）で動かして確認する。送受信のような状態を持つUIは、切り替えを繰り返すテストで
  リスナーリークやレースコンディションがないか確認する
- スクリーンショット・コンソールログ・pageerrorを能動的に確認し、疑わしい挙動は自分で深掘りする
- UIは「体験（結果を直感的に見る/聴く）」と「仕組みの理解（解説）」を分離する構成が有効
  （まず送受信を体感してもらい、下にスクロールすると仕組みの解説、という流れ）
- 実装終盤で、企画書（`Plan.md`）に書かれた教育目標とUIの各部分の対応関係を明示的に確認する
  フェーズを設ける
