# Step.md

SignalChordの実装ロードマップ。「動くものが一つ増える」粒度でフェーズを区切り、
チェックボックスで進捗管理する。各フェーズの実装後は一言メモ（何を作ったか、何が分かったか）を
追記していく。各フェーズの最後には実機（スマートフォン）での確認ステップを入れる。

参照: `Plan.md`（企画・UX・教育目標）, `science-app-framework.md`（共通開発フレームワーク）,
`CLAUDE.md`（本プロジェクトの開発指針）

---

## Phase 0: プロジェクトセットアップ

- [x] 技術スタックを決定する（TypeScript + 軽量ビルドツール、ブラウザのみで完結、GitHub Pages対応の構成）
- [x] リポジトリの雛形を作成する（`package.json`, ビルド設定, `.gitignore`等）
- [x] GitHub Pagesへの最小デプロイができることを確認する（空のページでよい）
- [x] 実機確認: スマートフォンから公開URLにアクセスできることを確認する

メモ: 技術スタックはVite + TypeScript（vanilla、フレームワークなし）に決定。UIがシンプルで
状態管理も複雑でない企画のため、React等は不要と判断。`npm run build`（tsc + vite build）と
`npm run dev`が正常に動作することを確認済み。GitHub Pagesのプロジェクトページ想定で
`vite.config.ts`に`base: "/SignalChord/"`を設定し、`.github/workflows/deploy.yml`で
mainブランチへのpushをトリガーに`actions/deploy-pages`でデプロイする構成を用意した。
`git init`し `git@github.com:kisakutanaka/SignalChord.git` にpush済み。リポジトリ側の
Settings→Pages→SourceをGitHub Actionsに切り替える作業はユーザー側で対応済み
（`gh` CLI未導入のため自動化できず手動対応）。デプロイ後、
`https://kisakutanaka.github.io/SignalChord/` に`curl`でアクセスし200 OK・
ビルド済みアセット（`/SignalChord/assets/...`）が正しく配信されることを確認した。
スマートフォン実機での表示確認もユーザーによって完了。Phase 0完了。

---

## Phase 1: 送信モード — 5音の生成・再生

- [x] Web Audio APIで5つの周波数（C5/D5/E5/G5/A5）をOscillatorで個別に鳴らせるようにする
- [x] 5bitのビットパターン（どの音を鳴らすか）を指定すると、該当する音だけが同時に再生される
      仕組みを作る
- [x] 最小UI: ボタンでテスト送信し、スピーカーから音が出ることを確認する
- [x] 実機確認: スマートフォンのスピーカーで実際に鳴ることを確認する

メモ: `src/audio/frequencies.ts`にNOTE_FREQUENCIES（C5〜A5）を定義し、`src/audio/transmitter.ts`の
`playPattern()`が5要素のBitPattern（NOTE_NAMESと同じ並び）を受け取り、trueの音だけを
OscillatorNode+GainNodeで同時再生する。複数音同時再生時のクリッピングを避けるため
1音あたりのgainを0.15に設定（5音全部でも1.0を超えない）。oscillatorの`ended`イベントで
disconnectし、再生のたびにノードが残り続けないようにした。最小UIとして5音のチェックボックスと
「テスト送信」ボタンをmain.tsに実装。Playwrightでページを起動し、コンソールエラー・pageerrorが
出ないこと、チェックボックス5個の描画とクリック操作が正常に動くことを確認済み（ヘッドレス
ブラウザのため実際の音の可聴確認はできていない）。スマートフォン実機でチェックボックスの選択に応じた音が実際に聞こえることをユーザーが確認。
Phase 1完了。

---

## Phase 2: 送信モードUI — 情報選択と周波数表示

- [x] 送りたい情報（文字等）を選択するUIを作る
- [x] 選択した情報を5bitのパターンに変換するロジックを作る（変換テーブル/エンコード方式を決める）
- [x] 5つの周波数を横に並べた表示（`C5 | D5 | E5 | G5 | A5`）を作り、使用中の音が視覚的に反応する
      ようにする
- [x] 実機確認: 送信操作から音・表示までの一連の流れをスマートフォンで確認する

メモ: `src/protocol/alphabet.ts`にA〜Z+空白+`.,!?`の31文字のALPHABETを定義し、
`charToIndex`/`indexToPattern`で1〜31のindexを5bitパターンに変換する（NOTE_NAMESの並びを
ビット順とする）。index 0（全ビットOFF=無音）は文字に割り当てず予約した（理由はFindings.md参照）。
`<select>`の各`<option>`には文字と一緒に鳴る音の組み合わせ（例:「C — C5+D5」）をヒントとして
埋め込み、選択肢自体から結果を予測できるようにした（science-app-framework.mdのUI指針に沿う）。
5つの周波数ボックスは選択中の文字に応じて`selected`（枠色）、送信中は`playing`（塗りつぶし+拡大）
の2段階で視覚的に反応する。PlaywrightでALPHABETが意図通り31個生成されること、"C"選択時に
C5+D5が正しくハイライトされること、送信中に`playing`クラスが付与されることをスクリーンショットで
確認済み。Phase 1の動作確認用チェックボックスUIはこのPhaseの本実装に置き換えた。
文字選択→音・表示の連動をスマートフォン実機でユーザーが確認。Phase 2完了。

---

## Phase 3: 受信モード — マイク入力とFFT解析

- [x] マイク入力を取得する（`getUserMedia`）
- [x] FFT（Web Audio APIの`AnalyserNode`等）で周波数スペクトルを取得する
- [x] 5つの対象周波数（C5/D5/E5/G5/A5）付近の信号強度からON/OFFを判定するロジックを作る
      （しきい値の決め方はFindings.mdに検証結果を残す）
- [x] 実機確認: 実際にスピーカーで鳴らした音をマイクで拾い、判定結果をログ等で確認する

メモ: `src/audio/receiver.ts`に`startReceiving()`（`getUserMedia`でマイク入力を取得。
音声通話向けのecho cancellation/noise suppression/auto gain controlはトーン検出に
悪影響を与えうるため無効化）と`measure()`（fftSize=4096の`AnalyserNode`から対象周波数
±1ビンのピークdBを読み、暫定しきい値-60dBでON/OFF判定）を実装。UIはmain.tsに
「受信テスト（開発中）」として最小限の可視化（周波数ボックスのハイライト+dB値の生ログ）を
追加した（本実装のUIはPhase 4）。ChromiumのFake Audio Capture機能（
`--use-file-for-fake-audio-capture`）でNode.js生成の合成音をマイク入力として与えるPlaywright
テストを実施し、C5+D5・全5音・単音（A5）・無音の各ケースで意図通りの検出結果を確認した
（詳細な数値はFindings.md参照）。この検証は理想的な合成音によるものであり、実機の
スピーカー/マイクでの再検証が必要。実機2台（送信/受信）でスピーカー→マイク経由の検出が
正しく反応することをユーザーが確認。Phase 3完了。

---

## Phase 4: 受信モードUI — 復元表示

- [x] 受信側にも5音の横並び表示を作り、受信できた音が視覚的に反応するようにする
- [x] 判定したビットパターンから元の情報（文字等）を復元して画面に表示する
- [ ] 必要に応じてFFTスペクトルの可視化を追加する（現時点ではdB値のテキストログのみ。
      本格的なスペクトル表示は不要と判断すれば見送る）
- [x] 実機確認: 2台のスマートフォン（送信/受信）で実際に通信し、情報が正しく復元されることを確認する

メモ: `src/protocol/decoder.ts`に`StreamDecoder`を実装。フレームごとのBitPatternを受け取り、
無音を挟まず同じ文字が続く間は重複確定させないステートフルなデコーダーで、受信側に
「現在の文字」表示と「受信履歴」の蓄積表示を追加した。実装直後にPlaywrightで無音を挟んだ
2文字連続送信（合成音）をテストしたところ、音の立ち上がり時のスペクトル漏れにより余分な
文字が混入するバグを発見・修正した（詳細と原因はFindings.md参照。安定判定フレーム数を
5→12に、送信側の音量変化を矩形→15msランプに変更）。修正後、"CP"のような複数文字の連続
送信が合成音テストで正しく復元されることを確認済み。実機2台（送信/受信）で文字送受信・履歴の蓄積が正しく動作することをユーザーが確認。
Phase 4完了。FFTスペクトル可視化は今のところ不要と判断し見送り。

---

## Phase 5: モード切り替えの統合

- [ ] 1つのアプリ内で送信モード/受信モードを切り替えられるUIにする
- [ ] モード切り替えを繰り返しても音声リスナー（マイク/Oscillator）がリークしないことを確認する
      （state管理のテスト）
- [ ] 実機確認: モード切り替えを繰り返しながら送受信が安定して動作することを確認する

メモ: (未着手)

---

## Phase 6: 発展的体験 — 周波数欠落のシミュレーション

- [ ] 特定の周波数だけを意図的に届かなくする（ミュートする）機能を作る
      （例: `C5 | D5 | × | G5 | A5`）
- [ ] 欠落があった場合に、対応する情報の一部が正しく復元されないことを体験できるUIにする
- [ ] 実機確認: 実際に欠落を発生させ、情報の一部が欠けることを確認する

メモ: (未着手)

---

## Phase 7: 教育目標との対応確認・仕上げ

- [ ] `Plan.md`の教育目標each項目について、UIのどの部分が対応しているかを明示的に確認する
- [ ] 「体験（結果を直感的に見る/聴く）」と「仕組みの理解（解説）」が分離された構成になっているか確認する
- [ ] UI/説明文が過不足なくシンプルであるかレビューする
- [ ] 実機確認: 一連の体験を通しで確認する（可能なら初見のユーザーによる確認）

メモ: (未着手)
