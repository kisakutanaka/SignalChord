# Findings.md

開発中に得られた学術的・科学的な発見や検証結果のログ。「なぜこの数値なのか」
「公表値・理論値とどれだけ合っているか」など、実装の副産物として得られる知見を都度追記する。
機能追加のたびに関連する発見がないか振り返り、あれば追記する（指示がなくても自発的に行う）。

参照: `Plan.md`（企画）, `CLAUDE.md`（開発指針）, `Step.md`（実装ロードマップ）

---

## 前提として企画時点で決まっている数値

- 通信に使用する5音はCメジャーペンタトニック: C5=523.25Hz, D5=587.33Hz, E5=659.25Hz,
  G5=783.99Hz, A5=880.00Hz。等間隔な周波数配置ではなく、同時に鳴らしても耳障りになりにくい
  音階が選ばれている（詳細な選定理由・等間隔配置との比較は未検証 — 実装時に必要なら検証する）。
- 5bit=32通りの状態を1回の送信で表現する方式（音の有無のON/OFFのみ、振幅・位相は変調しない）。

## Phase 2で分かったこと: 「全ビットOFF」は使えない状態

5bit=32通りのうち、5音すべてが鳴らない状態（index 0, 00000）は、「その状態を送信している」
ことと「何も送信していない（送信前・送信後の無音）」ことを、音だけでは区別できない。
音の有無のみで情報を表す単純な方式そのものに内在する制約であり、実装（`src/protocol/alphabet.ts`）
ではindex 0を文字には割り当てず予約し、使用可能な文字は31種類（index 1〜31）とした。
これは「情報の一部が正しく届かなくなる」発展的体験（Phase 6, 特定周波数の欠落）とは別の、
方式自体の限界として教育目標との対応付け（Phase 7）で触れる価値がある。

## Phase 3で検証したこと: FFT分離とゲイン設計（合成音・偽装マイク入力による自動検証）

`src/audio/receiver.ts`の実装（fftSize=4096, `AnalyserNode.getFloatFrequencyData`で
対象周波数±1ビンのピークdBを読む方式）を、Chromiumの`--use-file-for-fake-audio-capture`
機能で既知の合成音（Node.jsで生成したWAV）をマイク入力として与えて検証した（実マイク・実スピーカー
を介さない分、音響経路のノイズ・残響・非線形性は含まれないシミュレーションである点に注意）。

- サンプリングレート48000Hz・fftSize=4096時のビン幅は約11.7Hz。最も近接するC5–D5間
  （約64Hz差）でも、C5+D5トーン再生時にC5/D5はどちらも約-22dBで検出され、隣接するE5は
  -84.9dBと明確に分離された（約60dBの差）。誤ってD5がC5に、あるいはその逆に漏れ込む
  クロストークは確認されなかった。
- `playPattern`と同じゲイン（1音あたり0.15、5音同時で最大振幅0.738）で全5音を同時再生した
  場合、5音すべてが約-30dB前後で検出され、振幅がフルスケール1.0を超えないため
  （デジタル信号としては）クリッピングは発生しない設計であることを確認した。
- A5単音の場合、A5は-30.3dBで検出される一方、最も近いG5でも-104.0dBに留まり、全域で
  マージンが大きい。
- 無音（全ビットOFF相当の入力）ではどの周波数も検出されず、`levelsDb`は`-Infinity`
  （フロアノイズなし）になる。
- 以上より、暫定しきい値`DEFAULT_THRESHOLD_DB = -60`（`src/audio/receiver.ts`）は
  この合成音条件では信号(-22〜-30dB)とノイズフロア(-85dB以下)の中間に大きな余裕を持って
  位置しており、妥当な暫定値と言える。

## 検証待ちの項目（実機でのみ確認できる、Phase 3の自動検証ではカバーしていない事項）

- 実際のスマートフォンのスピーカー/マイクを介した場合の周波数特性・環境ノイズ・残響による
  誤判定率（上記の自動検証は理想的な合成音での結果であり、実機ではしきい値-60dBの妥当性を
  再検証する必要がある）
- 実スピーカーでの複数周波数同時再生時に生じうる非線形歪みによる相互変調（デジタル信号
  としてのクリッピングは発生しない設計だが、アナログ経路での歪みは別問題として実機確認が必要）
